class storage():
    while 1:
        def filter(self):
            #for useful data
        def tuple(self,bm,bo,p):
            try:
            #check if it is correct
                tup = ('bloodmeasure','bloodoxygen','pulse',bm,bo,p)
                return tup
            except:
                return 1

        def input(bo,bp,pul):
            #connection to the database
            '''
            format:
            bo,bp,pul are three lists represent the value of blood oxygen,blood pressure and pulse
            :param bp:
            :param pul:
            :return:
            '''
            #storage the data into the database
        def output(bo,bp,pul):
            #connection to the database
            '''
            format:
            bo,bp,pul are three lists represent the value of blood oxygen,blood pressure and pulse
            :param bp:
            :param pul:
            :return:
            '''
            #extract the data out of the database of the format
